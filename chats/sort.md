Just as we've done with `filter` in @ Typescript Filter implementation, we now need to ensure we implement the `sort` method for queries.

Please suggest how to proceed. Make sure you understand our @ rfp first.
