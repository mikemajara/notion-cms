# @notion-cms/notion-to-md

Early-stage utility for converting Notion block responses into Markdown. This package mirrors the markdown conversion logic used in `@mikemajara/notion-cms` so it can evolve independently.
